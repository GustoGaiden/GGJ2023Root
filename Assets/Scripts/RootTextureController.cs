using System;
using UnityEngine;

public class RootTextureController : MonoBehaviour
{
     
    
    void Start()
    {
        // Color[] loadedPixels = texture.GetPixels();
        // for (int i = 0; i < loadedPixels.Length; i++)
        // {
        //     loadedPixels[i] = new Color(1 - loadedPixels[i].r, 1 - loadedPixels[i].g, 1 - loadedPixels[i].b);
        // }
        // texture.SetPixels(loadedPixels);
        // texture.Apply();
        
    }

    private void Update()
    {
        
        //GUI.DrawTexture(new Rect(index, index, 32, 32), root);
    }

    private void OnGUI()
    {
        
    }

  
}
